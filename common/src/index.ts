interface Color {
  red: number;
  blue: number;
}

const color: Color = {
  red: 10,
  blue: 20,
};

console.log(color);
console.log("====================================");

console.log("====================================");
("hello word");

const otherColor: Color = {
  red: 30,
  blue: 111,
};

export default {
  color,
  otherColor,
};
